<!-- https://www.playframework.com/documentation/latest/SettingsLogger -->
<configuration>

    <property scope="context" name="log-pattern"
              value="${am.logback.pattern:-%date %highlight([%-4level]) [%-10.10thread] [%30.30logger]: %message%n%xException}"/>

    <appender class="ch.qos.logback.core.rolling.RollingFileAppender" name="ROLLING_FILE">
        <file>${am.logback.log_dir:-./logs}/app.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${am.logback.log_dir:-./logs}/%d{yyyy/MM,aux}/app_%d{yyyy-MM-dd}.%i.gz</fileNamePattern>
            <maxFileSize>50MB</maxFileSize>
            <maxHistory>${am.logback.file.max-history:-30}</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>${log-pattern}</pattern>
        </encoder>
    </appender>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${log-pattern}</pattern>
        </encoder>
    </appender>

    <appender name="ASYNCSTDOUT" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="STDOUT"/>
    </appender>

    <!-- https://hibernate.atlassian.net/browse/HV-1323 -->
    <logger name="org.hibernate.validator.messageinterpolation.ParameterMessageInterpolator" level="ERROR"/>
    <logger name="org.hibernate.validator" level="ERROR"/>

    <root level="debug">
        <appender-ref ref="${am.logback.output:-STDOUT}"/>
    </root>

</configuration>
